apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: music-charts
spec:
  type: ClusterIP
  ports:
  - port: 8501
    targetPort: 8501
    protocol: TCP
    name: http
  selector:
    app: dashboard

